// This is the splash screen that appears on application startup

import { View, Text } from "react-native";
import React from "react";

const SplashScreen = () => {
  return (
    <View>
      <Text>SplashScreen</Text>
    </View>
  );
};

export default SplashScreen;
